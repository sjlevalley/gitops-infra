---
- name: Join worker to cluster (placeholder - requires token from master)
  debug:
    msg: "Worker join command should be run using output from master-init role. Use kubeadm token create --print-join-command on master."
  when: false
